{
  "name": "polyhedge",
  "private": true,
  "description": "Express API (markets, Auth0 signup proxy) — run from repo root: npm install && npm start",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "engines": {
    "node": ">=18"
  }
}
